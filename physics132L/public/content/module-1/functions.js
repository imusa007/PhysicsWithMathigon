/* (c) Mathigon, generated by Mathigon Studio */
"use strict";
var StepFunctions = (() => {
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

  // content/module-1/functions.ts
  var functions_exports = {};
  __export(functions_exports, {
    additionInteractive: () => additionInteractive,
    crossInteractive: () => crossInteractive,
    dotInteractive: () => dotInteractive,
    introSlider: () => introSlider,
    subInteractive: () => subInteractive
  });
  var S = 340;
  var O = S / 2;
  var SC = 30;
  function bindVectorMath(model) {
    model.round = (x, n) => n !== void 0 ? Number(x.toFixed(n)) : Math.round(x);
    model.sqrt = Math.sqrt;
    model.abs = Math.abs;
    model.acos = Math.acos;
    model.min = Math.min;
    model.max = Math.max;
    model.pi = Math.PI;
  }
  var NS = "http://www.w3.org/2000/svg";
  var px = (v) => O + v * SC;
  function drawGrid(svg) {
    for (let i = -5; i <= 5; i++) {
      const vl = document.createElementNS(NS, "line");
      vl.setAttribute("x1", String(px(i)));
      vl.setAttribute("y1", "0");
      vl.setAttribute("x2", String(px(i)));
      vl.setAttribute("y2", String(S));
      vl.setAttribute("stroke", i === 0 ? "#555" : "#e0e0e0");
      vl.setAttribute("stroke-width", i === 0 ? "1.5" : "0.5");
      svg.appendChild(vl);
      const hl = document.createElementNS(NS, "line");
      hl.setAttribute("x1", "0");
      hl.setAttribute("y1", String(px(i)));
      hl.setAttribute("x2", String(S));
      hl.setAttribute("y2", String(px(i)));
      hl.setAttribute("stroke", i === 0 ? "#555" : "#e0e0e0");
      hl.setAttribute("stroke-width", i === 0 ? "1.5" : "0.5");
      svg.appendChild(hl);
      if (i !== 0) {
        const tx = document.createElementNS(NS, "text");
        tx.setAttribute("x", String(px(i)));
        tx.setAttribute("y", String(O + 14));
        tx.setAttribute("text-anchor", "middle");
        tx.setAttribute("font-size", "9");
        tx.setAttribute("fill", "#aaa");
        tx.textContent = String(i);
        svg.appendChild(tx);
        const ty = document.createElementNS(NS, "text");
        ty.setAttribute("x", String(O + 6));
        ty.setAttribute("y", String(px(-i) + 3));
        ty.setAttribute("font-size", "9");
        ty.setAttribute("fill", "#aaa");
        ty.textContent = String(i);
        svg.appendChild(ty);
      }
    }
  }
  function arrow(svg, x1, y1, x2, y2, color, label, uid) {
    const mid = `mk-${uid}`;
    const defs = document.createElementNS(NS, "defs");
    const mk = document.createElementNS(NS, "marker");
    mk.setAttribute("id", mid);
    mk.setAttribute("markerWidth", "8");
    mk.setAttribute("markerHeight", "8");
    mk.setAttribute("refX", "6");
    mk.setAttribute("refY", "3");
    mk.setAttribute("orient", "auto");
    const poly = document.createElementNS(NS, "polygon");
    poly.setAttribute("points", "0 0, 6 3, 0 6");
    poly.setAttribute("fill", color);
    mk.appendChild(poly);
    defs.appendChild(mk);
    svg.appendChild(defs);
    for (const [a, b, c, d] of [[x1, y2, x2, y2], [x1, y1, x1, y2]]) {
      const dl = document.createElementNS(NS, "line");
      dl.setAttribute("x1", String(a));
      dl.setAttribute("y1", String(b));
      dl.setAttribute("x2", String(c));
      dl.setAttribute("y2", String(d));
      dl.setAttribute("stroke", color);
      dl.setAttribute("stroke-dasharray", "4,3");
      dl.setAttribute("stroke-width", "1");
      dl.setAttribute("opacity", "0.35");
      svg.appendChild(dl);
    }
    const ln = document.createElementNS(NS, "line");
    ln.setAttribute("x1", String(x1));
    ln.setAttribute("y1", String(y1));
    ln.setAttribute("x2", String(x2));
    ln.setAttribute("y2", String(y2));
    ln.setAttribute("stroke", color);
    ln.setAttribute("stroke-width", "2.5");
    ln.setAttribute("marker-end", `url(#${mid})`);
    svg.appendChild(ln);
    const tx = document.createElementNS(NS, "text");
    tx.setAttribute("x", String(x2 + 7));
    tx.setAttribute("y", String(y2 - 5));
    tx.setAttribute("fill", color);
    tx.setAttribute("font-size", "13");
    tx.setAttribute("font-weight", "bold");
    tx.textContent = label;
    svg.appendChild(tx);
  }
  function parallelogram(svg, ax, ay, bx, by) {
    const poly = document.createElementNS(NS, "polygon");
    const pts = [[O, O], [px(ax), px(-ay)], [px(ax + bx), px(-(ay + by))], [px(bx), px(-by)]].map((p) => p.join(",")).join(" ");
    poly.setAttribute("points", pts);
    poly.setAttribute("fill", "rgba(21,101,192,0.1)");
    poly.setAttribute("stroke", "#1565C0");
    poly.setAttribute("stroke-width", "1");
    poly.setAttribute("stroke-dasharray", "5,3");
    svg.appendChild(poly);
  }
  function redraw(svgId, fn) {
    const svg = document.querySelector(`#${svgId}`);
    if (!svg) return;
    while (svg.firstChild) svg.removeChild(svg.firstChild);
    drawGrid(svg);
    fn(svg);
  }
  function introSlider($step) {
    bindVectorMath($step.model);
    $step.model.watch(() => {
      var _a, _b;
      const vx = (_a = $step.model.vx) != null ? _a : 3, vy = (_b = $step.model.vy) != null ? _b : 2;
      redraw("vector-intro", (svg) => arrow(svg, O, O, px(vx), px(-vy), "#E91E63", "v", "vi"));
    });
  }
  function additionInteractive($step) {
    $step.model.watch(() => {
      var _a, _b, _c, _d;
      const ax = (_a = $step.model.ax) != null ? _a : 2, ay = (_b = $step.model.ay) != null ? _b : 1;
      const bx = (_c = $step.model.bx) != null ? _c : 1, by = (_d = $step.model.by) != null ? _d : 3;
      redraw("vector-add", (svg) => {
        arrow(svg, O, O, px(ax), px(-ay), "#E91E63", "a", "aa");
        arrow(svg, px(ax), px(-ay), px(ax + bx), px(-(ay + by)), "#4CAF50", "b", "ab");
        arrow(svg, O, O, px(ax + bx), px(-(ay + by)), "#1565C0", "a+b", "asum");
      });
    });
  }
  function subInteractive($step) {
    $step.model.watch(() => {
      var _a, _b, _c, _d;
      const ax2 = (_a = $step.model.ax2) != null ? _a : 3, ay2 = (_b = $step.model.ay2) != null ? _b : 2;
      const bx2 = (_c = $step.model.bx2) != null ? _c : 1, by2 = (_d = $step.model.by2) != null ? _d : 3;
      redraw("vector-sub", (svg) => {
        arrow(svg, O, O, px(ax2), px(-ay2), "#E91E63", "a", "sa");
        arrow(svg, O, O, px(bx2), px(-by2), "#4CAF50", "b", "sb");
        arrow(svg, px(bx2), px(-by2), px(ax2), px(-ay2), "#FF9800", "a\u2212b", "sd");
      });
    });
  }
  function dotInteractive($step) {
    bindVectorMath($step.model);
    $step.model.watch(() => {
      var _a, _b, _c, _d;
      const dax = (_a = $step.model.dax) != null ? _a : 3, day = (_b = $step.model.day) != null ? _b : 1;
      const dbx = (_c = $step.model.dbx) != null ? _c : 1, dby = (_d = $step.model.dby) != null ? _d : 2;
      redraw("vector-dot", (svg) => {
        arrow(svg, O, O, px(dax), px(-day), "#E91E63", "a", "da");
        arrow(svg, O, O, px(dbx), px(-dby), "#4CAF50", "b", "db");
      });
    });
  }
  function crossInteractive($step) {
    bindVectorMath($step.model);
    $step.model.watch(() => {
      var _a, _b, _c, _d;
      const cax = (_a = $step.model.cax) != null ? _a : 3, cay = (_b = $step.model.cay) != null ? _b : 1;
      const cbx = (_c = $step.model.cbx) != null ? _c : 1, cby = (_d = $step.model.cby) != null ? _d : 2;
      redraw("vector-cross", (svg) => {
        parallelogram(svg, cax, cay, cbx, cby);
        arrow(svg, O, O, px(cax), px(-cay), "#E91E63", "a", "ca");
        arrow(svg, O, O, px(cbx), px(-cby), "#4CAF50", "b", "cb");
      });
    });
  }
  return __toCommonJS(functions_exports);
})();