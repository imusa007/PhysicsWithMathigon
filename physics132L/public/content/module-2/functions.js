/* (c) Mathigon, generated by Mathigon Studio */
"use strict";
var StepFunctions = (() => {
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

  // content/module-2/functions.ts
  var functions_exports = {};
  __export(functions_exports, {
    dataTable: () => dataTable
  });
  var NS = "http://www.w3.org/2000/svg";
  var W = 360;
  var H = 360;
  var PAD = { top: 20, right: 20, bottom: 50, left: 55 };
  var PW = W - PAD.left - PAD.right;
  var PH = H - PAD.top - PAD.bottom;
  function linFit(xs, ys) {
    const n = xs.length;
    if (n < 2) return null;
    const sx = xs.reduce((a, b) => a + b, 0);
    const sy = ys.reduce((a, b) => a + b, 0);
    const sxy = xs.reduce((a, b, i) => a + b * ys[i], 0);
    const sx2 = xs.reduce((a, b) => a + b * b, 0);
    const denom = n * sx2 - sx * sx;
    if (Math.abs(denom) < 1e-10) return null;
    const slope = (n * sxy - sx * sy) / denom;
    const intercept = (sy - slope * sx) / n;
    return { slope, intercept };
  }
  function scaleX(x, xMin, xMax) {
    return PAD.left + (x - xMin) / (xMax - xMin) * PW;
  }
  function scaleY(y, yMin, yMax) {
    return PAD.top + PH - (y - yMin) / (yMax - yMin) * PH;
  }
  function niceRange(vals) {
    const mn = Math.min(...vals), mx = Math.max(...vals);
    const pad = (mx - mn) * 0.15 || 1;
    return [mn - pad, mx + pad];
  }
  function el(tag, attrs) {
    const e = document.createElementNS(NS, tag);
    for (const [k, v] of Object.entries(attrs)) e.setAttribute(k, v);
    return e;
  }
  function drawAxes(svg, xMin, xMax, yMin, yMax) {
    svg.appendChild(el("rect", { x: "0", y: "0", width: String(W), height: String(H), fill: "#fafafa" }));
    for (let i = 0; i <= 5; i++) {
      const xv = xMin + i * (xMax - xMin) / 5;
      const xp = String(scaleX(xv, xMin, xMax));
      svg.appendChild(el("line", {
        x1: xp,
        y1: String(PAD.top),
        x2: xp,
        y2: String(PAD.top + PH),
        stroke: "#e0e0e0",
        "stroke-width": "0.8"
      }));
      const xt = el("text", {
        x: xp,
        y: String(PAD.top + PH + 18),
        "text-anchor": "middle",
        "font-size": "11",
        fill: "#888"
      });
      xt.textContent = xv.toFixed(1);
      svg.appendChild(xt);
      const yv = yMin + i * (yMax - yMin) / 5;
      const yp = String(scaleY(yv, yMin, yMax));
      svg.appendChild(el("line", {
        x1: String(PAD.left),
        y1: yp,
        x2: String(PAD.left + PW),
        y2: yp,
        stroke: "#e0e0e0",
        "stroke-width": "0.8"
      }));
      const yt = el("text", {
        x: String(PAD.left - 8),
        y: yp,
        "text-anchor": "end",
        "dominant-baseline": "middle",
        "font-size": "11",
        fill: "#888"
      });
      yt.textContent = yv.toFixed(1);
      svg.appendChild(yt);
    }
    svg.appendChild(el("line", {
      x1: String(PAD.left),
      y1: String(PAD.top),
      x2: String(PAD.left),
      y2: String(PAD.top + PH),
      stroke: "#555",
      "stroke-width": "1.5"
    }));
    svg.appendChild(el("line", {
      x1: String(PAD.left),
      y1: String(PAD.top + PH),
      x2: String(PAD.left + PW),
      y2: String(PAD.top + PH),
      stroke: "#555",
      "stroke-width": "1.5"
    }));
    const xl = el("text", {
      x: String(PAD.left + PW / 2),
      y: String(H - 4),
      "text-anchor": "middle",
      "font-size": "13",
      fill: "#333",
      "font-weight": "bold"
    });
    xl.textContent = "Time t (s)";
    svg.appendChild(xl);
    const yl = el("text", {
      x: "14",
      y: String(PAD.top + PH / 2),
      "text-anchor": "middle",
      "font-size": "13",
      fill: "#333",
      "font-weight": "bold",
      transform: `rotate(-90,14,${PAD.top + PH / 2})`
    });
    yl.textContent = "Velocity v (m/s)";
    svg.appendChild(yl);
  }
  function renderPlot(svg, xs, ys) {
    while (svg.firstChild) svg.removeChild(svg.firstChild);
    if (xs.length === 0) {
      svg.appendChild(el("rect", { x: "0", y: "0", width: String(W), height: String(H), fill: "#fafafa" }));
      const t = el("text", {
        x: String(W / 2),
        y: String(H / 2),
        "text-anchor": "middle",
        "font-size": "13",
        fill: "#bbb"
      });
      t.textContent = "Enter data to plot";
      svg.appendChild(t);
      return;
    }
    const [xMin, xMax] = niceRange(xs);
    const [yMin, yMax] = niceRange(ys);
    drawAxes(svg, xMin, xMax, yMin, yMax);
    const fit = linFit(xs, ys);
    if (fit) {
      const x1 = xMin, y1 = fit.slope * x1 + fit.intercept;
      const x2 = xMax, y2 = fit.slope * x2 + fit.intercept;
      svg.appendChild(el("line", {
        x1: String(scaleX(x1, xMin, xMax)),
        y1: String(scaleY(y1, yMin, yMax)),
        x2: String(scaleX(x2, xMin, xMax)),
        y2: String(scaleY(y2, yMin, yMax)),
        stroke: "#FF6F00",
        "stroke-width": "2",
        "stroke-dasharray": "6,3",
        opacity: "0.8"
      }));
    }
    for (let i = 0; i < xs.length; i++) {
      svg.appendChild(el("circle", {
        cx: String(scaleX(xs[i], xMin, xMax)),
        cy: String(scaleY(ys[i], yMin, yMax)),
        r: "5",
        fill: "#1565C0",
        stroke: "white",
        "stroke-width": "1.5"
      }));
    }
  }
  function updateResults(container, xs, ys) {
    const fit = linFit(xs, ys);
    if (!fit) {
      container.innerHTML = "<p>Enter at least 2 data points to see the fit.</p>";
      return;
    }
    const sign = fit.intercept >= 0 ? "+" : "\u2212";
    const intercept = Math.abs(fit.intercept).toFixed(3);
    container.innerHTML = `
    <p><strong>Best-fit line:</strong></p>
    <p class="fit-eq">v = ${fit.slope.toFixed(3)} t ${sign} ${intercept}</p>
    <p><strong>Slope (acceleration):</strong> ${fit.slope.toFixed(3)} m/s\xB2</p>
    <p><strong>y-intercept (v\u2080):</strong> ${fit.intercept.toFixed(3)} m/s</p>
    <p><em>N = ${xs.length} points</em></p>
  `;
  }
  function dataTable($step) {
    const container = $step._el;
    const readData = () => {
      const tInputs = Array.from(container.querySelectorAll(".t-val"));
      const vInputs = Array.from(container.querySelectorAll(".v-val"));
      const xs = [], ys = [];
      for (let i = 0; i < tInputs.length; i++) {
        const t = parseFloat(tInputs[i].value);
        const v = parseFloat(vInputs[i].value);
        if (!isNaN(t) && !isNaN(v)) {
          xs.push(t);
          ys.push(v);
        }
      }
      return { xs, ys };
    };
    const refresh = () => {
      const { xs, ys } = readData();
      const svg = container.querySelector("#vt-plot");
      const results = container.querySelector("#fit-results");
      if (svg) renderPlot(svg, xs, ys);
      if (results) updateResults(results, xs, ys);
    };
    setTimeout(() => {
      container.querySelectorAll(".t-val, .v-val").forEach((inp) => {
        inp.addEventListener("input", refresh);
      });
      refresh();
    }, 100);
  }
  return __toCommonJS(functions_exports);
})();